import { FREESTACK_REPOS } from '@/lib/freestack'
export default function Page(){
  return (<div className="min-h-screen bg-black text-white p-6"><div className="max-w-7xl mx-auto"><h1 className="text-4xl font-bold">10 Repos So Good They Shouldn't Be Free</h1><p className="text-gray-400 mt-2 mb-6">Hyperautomation Labs FREESTACK - $27k Bloomberg to $30 Superhuman free open-source alt - 100% free but needs API keys - Hostamar WSL ONLY store</p><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-4">{FREESTACK_REPOS.map(r=>(<div key={r.id} className="border border-green-500/30 bg-zinc-900 p-4 rounded"><div className="flex justify-between"><span className="text-green-400">#{String(r.id).padStart(2,'0')} {r.name}</span><span className="text-xs bg-red-900 px-2 py-1 rounded">{r.replaces} {r.price}</span></div><p className="text-sm mt-2">{r.what}</p><p className="text-xs text-gray-500 mt-2">Install: {r.install}</p><a href={r.url} target="_blank" className="text-xs text-blue-400 underline">{r.github}</a></div>))}</div></div></div>)
}
