'use client'
export default function AgentCloud() {
  return (
    <div className="p-6 max-w-4xl mx-auto">
      <h1 className="text-2xl font-bold">Hostamar AI Agent Cloud — agent.hostamar.com</h1>
      <p className="text-sm opacity-70 mt-2">
        Coolify PaaS + sim visual workflow + TencentDB-Agent-Memory + open-connector 1400+ APIs + persistent container + cron + storage + memory + GPU. Base44 solo $80M exit. Context Graph Atlan trillion opp.
      </p>
      <div className="mt-4 border rounded p-3 text-sm">
        <div className="font-semibold">Your Agent:</div>
        <div>My Agent daily morning YouTube trend → video → Facebook post. You give: persistent container + cron + file storage + memory + GPU. 19 employees Nova Forge Harbor building 24/7.</div>
      </div>
    </div>
  )
}
