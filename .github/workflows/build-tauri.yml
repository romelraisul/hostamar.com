name: Build Hostamar Node installers
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hostamar Node v0.1.0 tag pushed — Tauri build placeholder. Add icons + enable full matrix when scaffold complete."
