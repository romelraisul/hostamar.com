# V16.6 Master — All Above — Employees + TV + Paper + Buttons — Hermes Main

Date: 2026-09-16 10:30 +06. Full regression pass over everything shipped 2026-09-15/16. All claims measured live this pass.

## 0. Live Status — ALL GREEN

| Probe | Result |
|---|---|
| `tv.hostamar.com/master.m3u8` | **200** (0.44s) |
| old `/hls/tv/index.m3u8` | 404 — expected (dead pre-Sep-9 path) |
| hostamar.com | 200 (0.48s) |
| `/api/orchestrator/status` | `pcOnline:true`, `pending:0` |
| `/api/social/publish` | ok:true |
| TV auto-heal log | `HLS 200 OK` (10:26, 10:27 — continuous) |
| Fresh segments | `seg9815.m4s` @ 10:28 — encoder live |
| Queue round-trip | `v16.6-master-test` pushed → cron pulled → node executed → honest status → **pending:0** |

## 1. Power Grid + Queue — FIXED (11abd4f), regression-free

Three stacked bugs, all still fixed:
- `keepalive.sh` line 34: `echo "$job"` **quoted** (was unquoted → Bengali message spaces split JSON → jq `Invalid numeric literal` → `set -e` killed execution before any job ran).
- No `while true`/`sleep 300` inner loop (line 50 comment; cron owns the 5-min cadence).
- Absolute node path `/home/romel/.local/bin/node` (line 40); exec bit present.
- `execute-job.js`: honest per-type status (no fake `ok:true`).

Crontab: **7 lines with `/home/romel/.local/bin/node`, 0 with `/usr/bin/node`**. `/tmp/hostamar-cron.log` last modified **2026-09-15 20:40** — no new errors in 14h (pre-fix residue only). `vercel-guard-cleanup` → `gh workflow dispatch` verified working earlier.

## 2. TV Stack — 5 root causes FIXED (a1a9ed9), regression-free

- `tv-ffmpeg-vp9`: **active**, no `Requires`/`After=tv-rtmp` (dependency removed). Fresh fmp4 segments flowing (seg9815).
- `hostamar-tv-rtmp` podman container: **Up 11 hours** (template-mount + `--entrypoint nginx -g "daemon off;"` + 1935-only held). `start_tv_stack.sh` self-heals on reboot.
- `restream`: active, real Neon DATABASE_URL in `.env.local` (empty-override fixed → no more `destinations:[]`).
- `tv-db`: **disabled** (local postgres pruned; Neon only).
- HLS URL contract: `TV_HLS_URL` + `health_check_auto_heal.sh` + status API all point at `master.m3u8`.
- All 7 TV units active: tv-ffmpeg, tv-ffmpeg-vp9, tv-hls2, restream, tv-agent, tv-tunnel, tv-health-check.timer.
- Brain: 13 Tv* tables, `lib/tv/generator.ts` RSS→BullMQ, 23 `/api/tv/*` routes, playlist 3700/3700.

## 3. Vercel — 12-function cap FIXED (6a5f9c0), regression-free

- Deploys: **Ready ×2** (hostamar.com 200 @0.48s).
- `/api/mcp/catalog` + `/api/ai-services/catalog`: `force-dynamic` + `s-maxage` (no `revalidate=` flags).
- Functions payload: **5.7 MB** (limit 50 MB). vercel.json: 12 daily crons (none sub-daily).

## 4. Fleets — 49 inert + 16 disabled + 7 real

- `fleet.json`: **49** — inert registry, nothing reads per-agent status. Skipped 49-prompt rewrite (Ponytail rung 1: prompt theater).
- **WSL cron fleet (the real workers): 7 lines on `/home/romel/.local/bin/node`, 0 dead-path.** Monitor 6/6 probes green (incl. tv.hostamar.com + /api/tv/status + orchestrator). ceo/cto/marketing/content carry TV goals.
- **AutoClaw: heartbeat DISABLED** (`every:'0s'`) — intentional, Hermes is main IDE. 4 processes live, no autonomous jobs. `docs/v16-hermes-main-autoclaw-secondary.md`. Its auto-designer lost 55 files at the rate wall (00:22); Hermes recovered 65 files (879a0db).

## 5. Paper Theme + Buttons — contrast chain CLOSED, regression-free

Both checks green:
```
fix-paper-contrast.py --check  → OK: 0 invisible text tokens across 78 files
fix-green-buttons.py --check   → OK: all solid-green surfaces carry white text
```
Residual cream hexes (`#F6EBD2`, `#B8AFA3`): **0 files**.

Chain of root causes fixed:
- **V16.2 (879a0db)**: AutoClaw's 55-file paper conversion died uncommitted at a GLM rate wall; Hermes rebuilt it deterministically (`paper-theme-convert.py`, explicit palette mapping — not a Tailwind scale override). Caught 2 of its own bugs (regex couldn't match hyphenated `zinc-800` → 385 tokens skipped; `text-white` on brand surfaces must stay white). 65 files, 0 dark tokens left.
- **V16.3 (214bd63)**: the invisible text was 2 un-mapped cream/greige hexes — `text-[#F6EBD2]` 1.16:1 (invisible) and `text-[#B8AFA3]` 2.13:1 (fail) → ink `#1C1917` (17.2:1) / ink-soft `#57534E` (7.5:1). 44 tokens → 0.
- **V16.4 (fe92d23)**: brand regex missed `emerald-*` + `from-[#0E7C3A]` gradient → `text-white` on green buttons flipped to ink (1.2:1). `fix-green-buttons.py` pass1+2, 14+11+4 files.
- **V16.5 (8a8c00a)**: `layout.tsx:168` nav icon had its own ternary `isActive ? 'text-[#1C1917]'` — ink icon on the green active pill; green bg lived in the parent string so the per-string check missed it. Same in `chat-client.tsx:200`. Both fixed; checker hardened to catch the sibling-ternary class. Repo sweep: none remaining.
- **015357 hero pill**: pixel probe showed 2,287 white text pixels inside the pill — **already correct**; the screenshot was stale browser cache (V16.4 Ready ~01:47, screenshot 01:53).

## 6. Ponytail + ECC

- Ponytail installed + active (full). This pass applied the ladder: verify-don't-rebuild (everything was already fixed), no speculative work, one report.
- ECC workflow: plan → test → implement → review → verify → remember → improve. Each V16.x pass left behind a rerunnable `--check` (build-path guard) — that is the "remember" step.

## 7. Where To Start Next

**Everything is green. TV is LIVE, Paper is readable, Buttons are white-on-green, Power Grid is running, Deploys are Ready.**

The only item that needs *you*:
- **Multi-platform TV**: add YouTube + Facebook live stream keys via `/admin/tv/restream` (TvStreamDestination rows) or `vercel env add FACEBOOK_RTMP_URL / YOUTUBE_RTMP_URL` — restream then tees the live channel to both platforms instantly. TV is live now; keys make it multi-platform.
- Hard-refresh (Ctrl+Shift+R) `/dashboard`, `/admin/chat`, `/` to clear stale caches.

Optional/deferred (no action needed):
- `wrangler login` (interactive) to restore D1 visibility from WSL.
- FB Page Graph tokens only if Page-posting is wanted (RTMP alternative works).

## 8. Regression runbook (verified commands)

```bash
# TV
curl -s -o /dev/null -w "%{http_code}\n" https://tv.hostamar.com/master.m3u8   # 200
systemctl --user is-active tv-ffmpeg tv-ffmpeg-vp9 tv-hls2 restream tv-agent tv-tunnel
podman ps --format '{{.Names}}' | grep tv-rtmp

# Queue
curl -s https://hostamar.com/api/orchestrator/status | jq '{pcOnline, lastReport}'
tail -5 /tmp/hostamar-keepalive.log

# Vercel
vercel ls | head -6
grep -h force-dynamic app/api/mcp/catalog/route.ts

# Contrast (both must exit 0)
python3 scripts/fix-paper-contrast.py --check
python3 scripts/fix-green-buttons.py --check

# Fleets
crontab -l | grep -c '.local/bin/node'   # 7
```
