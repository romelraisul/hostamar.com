# 404 FIX REPORT — 2026-08-26

## Crawl results (live, post-fix)

| Route | Status | Note |
|---|---|---|
| /chat | 200 | public marketing chat — never deleted |
| /browser | 200 | public browser landing — never deleted |
| /ide | 200 | public IDE landing — never deleted |
| /game | 200 | public game landing — never deleted |
| /pricing, /about, /signup, /login, /blog | 200 | |
| /docs | **200** (was 404) — new index page + restored /docs/sops/* from git |
| /developers | **200** (was 404) — restored from git commit 31b61b3 |
| /dashboard/* (all) | 307 → /login unauthenticated = correct auth wall, not 404; 200 logged in |
| /.well-known/security.txt | 200 | |
| /sitemap.xml | 200 | all sitemap URLs crawled: **0 broken** |

## Root cause

NOT the dashboard refactor. Public pages /chat /browser /ide /game were never
deleted — they coexist with the dashboard wrappers at different routes.
The only real 404s were:
- `/developers` — deleted in a past refactor; restored via
  `git checkout 31b61b3 -- app/developers`
- `/docs` — only had /docs/sops subpages in git history, no index; created a clean
  `app/docs/page.tsx` docs-hub linking getting-started/products/developers

## Verification commands

```
for p in /chat /browser /ide /game /docs /developers; do curl -s -o /dev/null -w "%{http_code} $p\n" https://hostamar.com$p; done
# all 200
```

Dashboard wrappers and public pages are separate routes by design — both stay.
